\doxysection{Batalla\+Naval Class Reference}
\hypertarget{class_batalla_naval}{}\label{class_batalla_naval}\index{BatallaNaval@{BatallaNaval}}
\doxysubsubsection*{Public Member Functions}
\begin{DoxyCompactItemize}
\item 
\Hypertarget{class_batalla_naval_a2a1b9cca137ec66893c7b5728698561a}\label{class_batalla_naval_a2a1b9cca137ec66893c7b5728698561a} 
void {\bfseries jugar\+Batalla\+Naval} ()
\begin{DoxyCompactList}\small\item\em En este metodo basicamente se inicia el juego, donde se daran las indicaciones generales y los jugadores podran como tal utilizar el juego en consola. \end{DoxyCompactList}\item 
\Hypertarget{class_batalla_naval_a04d4207a5128c402adc232878918ce1d}\label{class_batalla_naval_a04d4207a5128c402adc232878918ce1d} 
void {\bfseries tablero\+Inicial} ()
\begin{DoxyCompactList}\small\item\em En este metodo basicamente inicializamos los tableros por comodida para que luego se pueda intercambiar o sustitur el agua por los barcos que se deseen colocar, vea que solo recorremos el array y le colocamos el agua. \end{DoxyCompactList}\item 
void \mbox{\hyperlink{class_batalla_naval_ab523fd3669603cabff33e49717ec6e81}{colc\+Barco}} (String\mbox{[}$\,$\mbox{]}\mbox{[}$\,$\mbox{]} tablero)
\begin{DoxyCompactList}\small\item\em En este metodo nos encargamos de "{}crear"{} los barcos, vea que implementamos una variable para saber el numero de barcos colocados, luego el ciclo de colocacion de barcos se repite 3 veces, luego registramos con un scanner la fila y columna de los barcos, para poder ver si dicha posicion se encuentra en el tablero con un if para ver si se puede colocar o no el barco. Posteriormente si cumple con las condiciones adecuadas, se coloca un cuadrito en el tablero que representara los barcos y se suma a la variable de barcos colocados. \end{DoxyCompactList}\item 
void \mbox{\hyperlink{class_batalla_naval_ac21cc0a872ca1092012efa8cd0c5a8c6}{mostrar\+Tab}} (String\mbox{[}$\,$\mbox{]}\mbox{[}$\,$\mbox{]} tablero)
\begin{DoxyCompactList}\small\item\em Este metodo se encarga de basicamente mostrar los tableros. Note que lo que hacemos en el metodo es el diseño "{}estetico"{} de los tablero, para ello dejamos la esquina superior izquierda vacia con el print inicial, luego recorremos las columnas del tablero para colocar el numero de columna y separarlos, luego metemos un salto de linea para que se logre poner de forma adecuada los numeros de las filas, se reitera el proceso de las columnas con las filas. Por ultimo, mostramos lo que tiene cada espacio de la matriz, vea que esto se hace paralelamente con las filas, pues estamos recorriendolas con un solo for por lo que requerimos mostrar lo que "{}tiene"{} cada tablero y como ya los inicilizamos y colocamos los barcos deben de mostrar dichas simbologias, note que se implemente un salto de linea por cada fila pues si no no se podria ver el orden del "{}tablero"{} porque estaria todo pegado. \end{DoxyCompactList}\item 
String \mbox{\hyperlink{class_batalla_naval_a970998a710aea3d17159add8c731f495}{disparar}} (String\mbox{[}$\,$\mbox{]}\mbox{[}$\,$\mbox{]} tablero\+Impacto, String\mbox{[}$\,$\mbox{]}\mbox{[}$\,$\mbox{]} vista\+Tab\+Contrincante)
\begin{DoxyCompactList}\small\item\em Metodo para disparar a los tableros Vea que este metodo se maneja mediante un while que se reitera infinitamente hasta devolver un impacto o un fallo esto es practico para hacer un bucle de turnos hasta que alguno de los jugadores destruya todos los barcos del otro mediante el registro de impactos. Note que en el while se registran las posiciones por disparar mediante scanners, luego se verifican que dichas posiciones esten en el tablero que entra como parametro, inclusive se utilizo un continue para evitar que la consola cierre el programa y reitere la peticion de coordenadas hasta que esten en el intervalo acordado. Luego se verifica si la posicion donde se dispara ya se disparo anterioremente, si ese es el caso se reitera la peticion de coordenadas cone l continue. \end{DoxyCompactList}\item 
\Hypertarget{class_batalla_naval_a45d81d6173b54e4fb4d80aa0ee5637ad}\label{class_batalla_naval_a45d81d6173b54e4fb4d80aa0ee5637ad} 
void {\bfseries iniciar\+Turnos} ()
\begin{DoxyCompactList}\small\item\em Metodo de Iniciar turnos\+: En este metodo basicamente se genera el sistema de turnos entre jugadores, note que establecimos tres variables donde vemos que la cantidad de barcos de cada jugador es de tres, y un boolean, esto para facilitar el conocimiento sobre cual jugador recibe su turno, pues solo asignamos que true es cuando juega el jugador 1 y false es el turno del jugador 2. Ahora bien hacemos un while para repetir el sistema de turnos hasta que alguno de los jugadores no tenga barcos en pie. Con los if diseñamos cada escenario, en el jugador 1 mostramos los tableros, tanto el tablero del jugador como el tablero del enemigo, esto por medio de la funcion mostrar\+Tabs donde true asume que el turno es del jugador 1, esta funcion se explicara despues. Posterior a ello llamamos a una variable tipo String (de alli la importancia de los returns como strings en el metodo anterior) que va a ser el resultado del metodo disparar empleado, que en este caso vea que dispara en el tablero2 y en vista\+Tab1 pues es el turno del jugador 1, ello para actualizarlos de forma simultanea. Ahora bien, se construye la condicional sobre si esta variable que es el resultado del metodo es un impacto, si es ese el caso disminuimos el numero de barcos del jugador 2 y se repite el turno del jugador 1 pues esta en un while. Si el resultado del metodo disparo es decir la variable resultadodisp es fallo, o cualquier otro distinto a impacto se asume que el jugador 1 fallo su disparo y se procede a repetir este mismo proceso descrito para el jugador 2. Esto hasta que alguno de los dos jugadores se quede sin barcos, que dado sea ese el caso se muestra un mensaje indicando el ganador en dependencia de cual jugador se quedo con un total de cero barcos. \end{DoxyCompactList}\item 
void \mbox{\hyperlink{class_batalla_naval_a4dc96bf237fc41520c8da598076f2f97}{mostrar\+Tabs}} (boolean jug1)
\begin{DoxyCompactList}\small\item\em Metodo para mostrar tableros actualizados en cada turno. Vea que este metodo es llamado en Iniciar\+Turno basicamente lo que hace es declarar dos tableros, uno que representara el tablero del jugador activo en el turno y otro que representa el tablero del enemigo con base en el jugador que este activo en el turno, esto evita tener que repetir este metodo para cada jugador en especifico. Vea que posterior a la declaracion de los tableros, asumimos que juega el jugador uno por medio de un if y en dado caso asigamos sus tableros a los arrays declarados, para luego llamar la funcion mostrar\+Tab y mostrar ambos tableros en consola, al utilizar un if, simplemente si no es el turno del jugador 1 va a ser el turno del jugador 2 y representa el mismo proceso antes descrito. Con lo cual en cada turno se mostraran los tableros deseados. \end{DoxyCompactList}\item 
int \mbox{\hyperlink{class_batalla_naval_a38449bcbc26b79611f42050c4b242c5d}{contador\+Barcos}} (String\mbox{[}$\,$\mbox{]}\mbox{[}$\,$\mbox{]} cualquier\+Tab)
\begin{DoxyCompactList}\small\item\em Metodo de contar barcos\+: Note que este metodo simplemente muestra los barcos con vida en el tablero de cada jugador, por ende, iniciamos el metodo con una variable que registre el numero de barcos de cada jugador, luego lo que hacemos es que este metodo recorra todo el tablero que se establecio de parametro, que despues solo registrara la cantidad de barcos en el tablero1 o tablero2. Luego suma 1 a la variable si encuentra una casilla con un barco y por ultimo cuando ya recorre todo el tablero retorna el contador de barcos. \end{DoxyCompactList}\end{DoxyCompactItemize}


\doxysubsection{Member Function Documentation}
\Hypertarget{class_batalla_naval_ab523fd3669603cabff33e49717ec6e81}\index{BatallaNaval@{BatallaNaval}!colcBarco@{colcBarco}}
\index{colcBarco@{colcBarco}!BatallaNaval@{BatallaNaval}}
\doxysubsubsection{\texorpdfstring{colcBarco()}{colcBarco()}}
{\footnotesize\ttfamily \label{class_batalla_naval_ab523fd3669603cabff33e49717ec6e81} 
void Batalla\+Naval.\+colc\+Barco (\begin{DoxyParamCaption}\item[{String}]{tablero}{\mbox{[}$\,$\mbox{]}\mbox{[}$\,$\mbox{]}}\end{DoxyParamCaption})}



En este metodo nos encargamos de "{}crear"{} los barcos, vea que implementamos una variable para saber el numero de barcos colocados, luego el ciclo de colocacion de barcos se repite 3 veces, luego registramos con un scanner la fila y columna de los barcos, para poder ver si dicha posicion se encuentra en el tablero con un if para ver si se puede colocar o no el barco. Posteriormente si cumple con las condiciones adecuadas, se coloca un cuadrito en el tablero que representara los barcos y se suma a la variable de barcos colocados. 


\begin{DoxyParams}{Parameters}
{\em tablero} & note que el parametro que recibimos es un tablero, pues como en el inicio declaramos los arrays de tabler1 y tablero2 estos seran los que se actualizaran con los barcos colocados, por lo cual, el metodo debe de recibir de parametro el tablero1 o el tablero2. \\
\hline
\end{DoxyParams}
\Hypertarget{class_batalla_naval_a38449bcbc26b79611f42050c4b242c5d}\index{BatallaNaval@{BatallaNaval}!contadorBarcos@{contadorBarcos}}
\index{contadorBarcos@{contadorBarcos}!BatallaNaval@{BatallaNaval}}
\doxysubsubsection{\texorpdfstring{contadorBarcos()}{contadorBarcos()}}
{\footnotesize\ttfamily \label{class_batalla_naval_a38449bcbc26b79611f42050c4b242c5d} 
int Batalla\+Naval.\+contador\+Barcos (\begin{DoxyParamCaption}\item[{String}]{cualquier\+Tab}{\mbox{[}$\,$\mbox{]}\mbox{[}$\,$\mbox{]}}\end{DoxyParamCaption})}



Metodo de contar barcos\+: Note que este metodo simplemente muestra los barcos con vida en el tablero de cada jugador, por ende, iniciamos el metodo con una variable que registre el numero de barcos de cada jugador, luego lo que hacemos es que este metodo recorra todo el tablero que se establecio de parametro, que despues solo registrara la cantidad de barcos en el tablero1 o tablero2. Luego suma 1 a la variable si encuentra una casilla con un barco y por ultimo cuando ya recorre todo el tablero retorna el contador de barcos. 


\begin{DoxyParams}{Parameters}
{\em cualquier\+Tab} & este parametro basicamente representa los tableros del jugador 1 y 2 que son los que nos interesan pues este metodo registra el numero de barcos con vida de cada jugador en tiempo real. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
note que el return simplemente retorna el numero de barcos en los tableros. 
\end{DoxyReturn}
\Hypertarget{class_batalla_naval_a970998a710aea3d17159add8c731f495}\index{BatallaNaval@{BatallaNaval}!disparar@{disparar}}
\index{disparar@{disparar}!BatallaNaval@{BatallaNaval}}
\doxysubsubsection{\texorpdfstring{disparar()}{disparar()}}
{\footnotesize\ttfamily \label{class_batalla_naval_a970998a710aea3d17159add8c731f495} 
String Batalla\+Naval.\+disparar (\begin{DoxyParamCaption}\item[{String}]{tablero\+Impacto}{\mbox{[}$\,$\mbox{]}\mbox{[}$\,$\mbox{]}, }\item[{String}]{vista\+Tab\+Contrincante}{\mbox{[}$\,$\mbox{]}\mbox{[}$\,$\mbox{]}}\end{DoxyParamCaption})}



Metodo para disparar a los tableros Vea que este metodo se maneja mediante un while que se reitera infinitamente hasta devolver un impacto o un fallo esto es practico para hacer un bucle de turnos hasta que alguno de los jugadores destruya todos los barcos del otro mediante el registro de impactos. Note que en el while se registran las posiciones por disparar mediante scanners, luego se verifican que dichas posiciones esten en el tablero que entra como parametro, inclusive se utilizo un continue para evitar que la consola cierre el programa y reitere la peticion de coordenadas hasta que esten en el intervalo acordado. Luego se verifica si la posicion donde se dispara ya se disparo anterioremente, si ese es el caso se reitera la peticion de coordenadas cone l continue. 


\begin{DoxyParams}{Parameters}
{\em tablero\+Impacto} & Note que este parametro representa los tableros 1 y 2 de los jugadores pues son los que reciben los impactos y registran si un barco fue destruido. \\
\hline
{\em vista\+Tab\+Contrincante} & Vea que en este caso debemos de utilizar dos parametros, en este se representaran a los tableros con la vista del enemigo para poder actualizar en tiempo real los tableros mostrados en consola con el pasar de cada turno. \\
\hline
\end{DoxyParams}
\begin{DoxyReturn}{Returns}
en continuidad con la explicacion del metodo, para los returns si la posicion disparada fue un barco con el if retornamos un impacto que indica que se le dio a un barco y se sustituye el cuadrito por una X que indica que se destruyo el barco. Si el disparo dio en el agua se retorna un fallo, como indicacion de que el jugador fallo su disparo. Note que este registro se hace de forma paralela para los tableros de los jugadores como para la vista de los tableros enemigos por la utilidad de los parametros, lo cual ahorro bastante codigo. Ademas por facilidad los returns son strings, pues simplemente se llaman mediante .equals. 
\end{DoxyReturn}
\Hypertarget{class_batalla_naval_ac21cc0a872ca1092012efa8cd0c5a8c6}\index{BatallaNaval@{BatallaNaval}!mostrarTab@{mostrarTab}}
\index{mostrarTab@{mostrarTab}!BatallaNaval@{BatallaNaval}}
\doxysubsubsection{\texorpdfstring{mostrarTab()}{mostrarTab()}}
{\footnotesize\ttfamily \label{class_batalla_naval_ac21cc0a872ca1092012efa8cd0c5a8c6} 
void Batalla\+Naval.\+mostrar\+Tab (\begin{DoxyParamCaption}\item[{String}]{tablero}{\mbox{[}$\,$\mbox{]}\mbox{[}$\,$\mbox{]}}\end{DoxyParamCaption})}



Este metodo se encarga de basicamente mostrar los tableros. Note que lo que hacemos en el metodo es el diseño "{}estetico"{} de los tablero, para ello dejamos la esquina superior izquierda vacia con el print inicial, luego recorremos las columnas del tablero para colocar el numero de columna y separarlos, luego metemos un salto de linea para que se logre poner de forma adecuada los numeros de las filas, se reitera el proceso de las columnas con las filas. Por ultimo, mostramos lo que tiene cada espacio de la matriz, vea que esto se hace paralelamente con las filas, pues estamos recorriendolas con un solo for por lo que requerimos mostrar lo que "{}tiene"{} cada tablero y como ya los inicilizamos y colocamos los barcos deben de mostrar dichas simbologias, note que se implemente un salto de linea por cada fila pues si no no se podria ver el orden del "{}tablero"{} porque estaria todo pegado. 


\begin{DoxyParams}{Parameters}
{\em tablero} & note que nuevamente unicamente se recibe de parametro un tablero, en este caso se utilizara tanto para la vista de enemigo como para los tableros de los jugadores, para mostrarlos en consola. \\
\hline
\end{DoxyParams}
\Hypertarget{class_batalla_naval_a4dc96bf237fc41520c8da598076f2f97}\index{BatallaNaval@{BatallaNaval}!mostrarTabs@{mostrarTabs}}
\index{mostrarTabs@{mostrarTabs}!BatallaNaval@{BatallaNaval}}
\doxysubsubsection{\texorpdfstring{mostrarTabs()}{mostrarTabs()}}
{\footnotesize\ttfamily \label{class_batalla_naval_a4dc96bf237fc41520c8da598076f2f97} 
void Batalla\+Naval.\+mostrar\+Tabs (\begin{DoxyParamCaption}\item[{boolean}]{jug1}{}\end{DoxyParamCaption})}



Metodo para mostrar tableros actualizados en cada turno. Vea que este metodo es llamado en Iniciar\+Turno basicamente lo que hace es declarar dos tableros, uno que representara el tablero del jugador activo en el turno y otro que representa el tablero del enemigo con base en el jugador que este activo en el turno, esto evita tener que repetir este metodo para cada jugador en especifico. Vea que posterior a la declaracion de los tableros, asumimos que juega el jugador uno por medio de un if y en dado caso asigamos sus tableros a los arrays declarados, para luego llamar la funcion mostrar\+Tab y mostrar ambos tableros en consola, al utilizar un if, simplemente si no es el turno del jugador 1 va a ser el turno del jugador 2 y representa el mismo proceso antes descrito. Con lo cual en cada turno se mostraran los tableros deseados. 


\begin{DoxyParams}{Parameters}
{\em jug1} & Note que este parametro lo que indica es cual jugador esta por observar sus tableros, si es true el jugador uno ve sus respectivos tableros y lo mismo para el jugador 2 si es false. \\
\hline
\end{DoxyParams}


The documentation for this class was generated from the following file\+:\begin{DoxyCompactItemize}
\item 
C\+:/\+Users/\+JOSUE-\/\+RA/\+CI-\/0112/\+Proyecto1/Batalla\+Naval.\+java\end{DoxyCompactItemize}
